services:
  db:
    build: ./db
    restart: unless-stopped
    environment:
      POSTGRES_USER: runbandits
      POSTGRES_PASSWORD: runbandits
      POSTGRES_DB: runbandits
    volumes:
      - ./backend/db/init_db.sql:/docker-entrypoint-initdb.d/init_db.sql
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U runbandits"]
      interval: 5s
      timeout: 5s
      retries: 5
